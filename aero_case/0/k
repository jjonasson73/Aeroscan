/*--------------------------------*- C++ -*----------------------------------*\
  AeroScan rider+bike case  (OpenFOAM ESI v2312+, openfoam.com)
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volScalarField;
    location    "0";
    object      k;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

#include "include/initialConditions"
dimensions      [0 2 -2 0 0 0 0];
internalField   uniform $turbKE;
boundaryField
{
    #includeEtc "caseDicts/setConstraintTypes"
    inlet
    {
        type fixedValue; value uniform $turbKE;
    }
    outlet
    {
        type inletOutlet; inletValue uniform $turbKE; value uniform $turbKE;
    }
    ground
    {
        type kqRWallFunction; value uniform $turbKE;
    }
    rider
    {
        type kqRWallFunction; value uniform $turbKE;
    }
    bike
    {
        type kqRWallFunction; value uniform $turbKE;
    }
    wheel_rear
    {
        type kqRWallFunction; value uniform $turbKE;
    }
    wheel_front
    {
        type kqRWallFunction; value uniform $turbKE;
    }
}
