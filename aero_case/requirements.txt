# Geometry build (build_model.py) and photo validation (overlay.py).
# scipy is not a hard dependency of trimesh but convex_hull needs it, so pin it here.
numpy>=1.24
scipy>=1.10
trimesh>=4.0
manifold3d>=2.3
shapely>=2.0
pillow>=10.0
# trimesh imports networkx lazily for several graph operations
networkx>=3.0
